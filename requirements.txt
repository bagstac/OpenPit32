# Runtime for scripts/login_service.py (Python 3.12+). Install into a venv:
#   python -m venv .venv312 && .venv312\Scripts\pip install -r requirements.txt
#
# Phase 8 (docs/ESP32_FIRMWARE_PLAN.md) retired the whole BLE stack this file
# used to pin (pytboss, bleak, bleak-retry-connector, bleak-esphome,
# aioesphomeapi, habluetooth) along with pywebpush/VAPID — the ESP32 firmware
# (esphome/grill-firmware.yaml) now owns 100% of grill control, alarms, and
# Telegram notifications directly. login_service.py just needs an HTTP
# server for the login form/session cookie/nginx auth_request target.
aiohttp==3.14.3
