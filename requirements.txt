# Runtime for scripts/ (Python 3.12+). Install into a venv:
#   python -m venv .venv312 && .venv312\Scripts\pip install -r requirements.txt
#
# Pinned to the versions this was developed and tested against; esphome_ble.py
# relies on private hooks of bleak-esphome (see its docstring), so bump that
# one deliberately.
pytboss==2026.8.6
aiohttp==3.14.3
bleak==3.0.2
bleak-retry-connector==4.7.0
bleak-esphome==4.1.0
aioesphomeapi==46.3.0
habluetooth==6.26.11
